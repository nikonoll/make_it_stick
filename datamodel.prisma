type User {
  id: ID! @id
  email: String @unique
  name: String!
}

type Deck {
  id: ID! @id
  name: String! @unique
  description: String!
  cards: [Card!]!
}

type Card {
  id: ID! @id
  question: String!
  answer: String!
  deck: Deck! @relation(link: INLINE)
}

